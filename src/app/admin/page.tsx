import { getServerSession } from "next-auth"; import { authOptions } from "@/lib/auth"; import { prisma } from "@/lib/prisma"; import { redirect } from "next/navigation"; import NewMangaForm from "./NewMangaForm"; import MangaListItem from "./MangaListItem";
export const dynamic="force-dynamic";
export default async function AdminPage(){const session=await getServerSession(authOptions);const role=(session?.user as any)?.role;if(!session?.user)redirect("/login");if(role!=="ADMIN")redirect("/");const list=await prisma.manga.findMany({include:{chapters:true},orderBy:{createdAt:"desc"}});return <div className="min-h-screen bg-[#fffafb]"><div className="mx-auto grid max-w-[1500px] gap-6 px-6 py-7 xl:grid-cols-[220px_1fr_360px]"><aside className="panel h-fit p-4 xl:sticky xl:top-28"><h2 className="px-3 pb-4 font-display text-xl font-bold text-[#ef7095]">Admin Panel</h2>{["แดชบอร์ด","จัดการมังงะ","จัดการตอน","จัดการหมวดหมู่","จัดการผู้ใช้","จัดการคอมเมนต์"].map((x,i)=><div key={x} className={`mb-1 rounded-xl px-4 py-3 text-sm ${i===1?"bg-[#fde4ec] font-semibold text-[#df6488]":"text-[#805764]"}`}>{x}</div>)}<a href="/admin/settings" className="mb-1 block rounded-xl px-4 py-3 text-sm text-[#805764] hover:bg-[#fff2f6]">ตั้งค่าเว็บไซต์</a></aside><section><div className="mb-5"><h1 className="font-display text-2xl font-bold">จัดการมังงะ</h1><p className="mt-1 text-sm text-[#a98591]">จัดการเรื่องทั้งหมดในระบบ</p></div><div className="mb-5 grid grid-cols-2 gap-4 lg:grid-cols-4">{[["มังงะทั้งหมด",list.length],["กำลังอัพ",list.filter(m=>m.status==="ongoing").length],["จบแล้ว",list.filter(m=>m.status==="completed").length],["ติดราคา",list.filter(m=>m.saleMode!=="FREE").length]].map(([a,b])=><div className="panel p-5" key={a}><p className="text-xs text-[#aa8490]">{a}</p><p className="mt-2 text-3xl font-bold text-[#714552]">{b}</p></div>)}</div><div className="panel overflow-hidden"><div className="flex flex-wrap gap-3 border-b border-[#f3dde4] p-4"><input className="soft-input max-w-xs" placeholder="ค้นหามังงะ..."/><select className="soft-input max-w-[180px]"><option>สถานะทั้งหมด</option><option>กำลังอัพ</option><option>จบแล้ว</option></select></div><div className="divide-y divide-[#f5e4e9]">{list.map(m=><MangaListItem key={m.id} slug={m.slug} title={m.title} coverUrl={m.coverUrl} category={m.category} status={m.status} saleMode={m.saleMode} fullPrice={m.fullPrice} chapterCount={m.chapters.length}/>)}</div></div></section><aside><NewMangaForm/></aside></div></div>}
